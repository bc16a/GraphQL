let createServer;_bb4‍.w("http",[["createServer",["createServer"],function(v){createServer=v}]]);

const server = createServe();

server.listen(8000, '127.0.0.1', ()=>{
    console.log('Server is listenning at http://127.0.0.1:8000');
});